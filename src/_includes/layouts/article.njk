---
layout: "layouts/base.njk"
---

<header id="header" class="article">
  <h1>{{ title }}</h1>
  <p class="meta">{{ page.date | shortDate }} en {{ category }}</p>
</header>

{{ content | toc | safe }}

{{ content | safe }}
